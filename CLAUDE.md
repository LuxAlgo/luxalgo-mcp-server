# Claude Code instructions

The repository's agent instructions live in `AGENTS.md` (imported below so there is a single source of truth); the authentication explainer every tool author must read is `docs/auth.md`.

@AGENTS.md
